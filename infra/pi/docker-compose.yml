version: "3.9"

services:
  web:
    image: eliasbe123/elias-portfolio:latest
    container_name: elias-portfolio
    restart: unless-stopped
    ports:
      - "80:3000"   # Maps Pi port 80 → container port 3000
    environment:
      NODE_ENV: production
